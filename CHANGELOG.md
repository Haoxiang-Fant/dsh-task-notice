# Changelog

## 0.3.1 (2026-09)

- **顶部导航新增「设置」选项卡**:原「消耗统计」视图中的插件配置项(插件总开关 / 每轮任务弹窗 / 目标完成弹窗 / 权限请求提醒 / 提问提醒 / 弹窗时长 / 浏览器系统通知 / 仅后台发送 / 账本保留天数)移至「设置」视图;新增**清除记录数据**——滑条拖到底 → 输入随机 8 位英文数字验证码 → 连续点击两次确认才执行清除(服务端 clearUsage 清空 usage.json,价格/套餐/峰谷配置保留;清除后统计与分析自动刷新)。
- **修复消耗归因「张冠李戴」**:旧版按 provider 硬编码命名空间取 apiKeyEnv,把 scnet 等 provider 的调用误记到共享命名空间(llm-deepseek)的 DEEPSEEK_API_KEY 名下;现按**真实路由配置**解析(扫描 settings 的 llm-* 命名空间 providers[route].apiKeyEnv → llm-<route> 扁平命名空间 → 路由专属环境变量),启动时对历史账本**按新解析器重算一次归属**(幂等),每次调用记录的模型名与 Key 均为该次调用实际使用的值。
- **默认价格目录对齐《主流大模型API官方价格汇总.xlsx》(2026-09)**:内置官方价目录扩至 80+ 模型,表格已收录的模型默认价一律按表格取值(输入价→缓外输入、缓存命中价→缓存命中、输出价→输出;区间价取基础起步价;DeepSeek 峰档 = 2× 谷档),并新增字节 Seed/Doubao、百度 ERNIE、腾讯混元、智谱 GLM-4-Plus / 4-Air / 4.7-Flash(免费)、月之暗面 K3 / K2.6 / K2.5 / Moonshot、阿里 Qwen3.7-Max / 3.6-Flash、小米 MiMo、零一 Yi-Lightning、阶跃 Step-3.7-Flash、百川 Baichuan-M3-Plus、讯飞 Spark、OpenAI GPT-5.4-Nano、Claude Fable 5 / Opus 5 / Sonnet 5、Gemini 3.6 / 3.5 Flash 系列等条目;DeepSeek V3.1 / R1 独立成行;表格未收录的既有官方价条目保留补充。
- **顶部导航栏新增「使用分析」界面**:在原「消耗统计 / 价格编辑」导航基础上新增「使用分析」页,分析规定时间内各大模型的使用资金状况,包含四个模块:
  - **消耗金额展示**:模型消耗金额占比**饼状图**(鼠标悬停饼图块浮现该模型具体使用情况:调用次数、各类 tokens、金额与占比)+ 饼图下方各 Key 消耗额度表格;
  - **Token Plan 回本提示**:每个设置为 Token Plan 的 Key 一张独立子卡片(Key 名称、名下每个模型的消耗金额、是否回本、每 Credit 对应 tokens、**重置当前周期**按钮);
  - **性价比计算排名**:所有模型的 **(Key · 模型)** 一起排名比较(排名名 = Key · 模型),按 1 元(CNY)能兑换多少 tokens 从高到低排序;**Token Plan 的模型按「订阅费 × 该模型 tokens ÷ 周期总 tokens」加权平均分配额度**(基于 Tokens 消耗摊薄订阅费用),优先用**上一轮计费周期**,无上一轮数据时用当前周期并标注「正在统计」;
  - **每日费用消耗表**:**微型日历**中代表一天的正方形填充颜色深度区分每天用量,日历下方列出每个日期的各模型消耗金额(Token Plan 模型按**加权平均**计算:订阅费 ÷ 周期总 tokens × 当天 tokens),数据默认堆叠,一次显示最近 6 天,滚轮滚动查看其余。
- **Token Plan 计费周期**:设置为 Token Plan 时需设置**套餐起效时间**与**有效期**(默认按 31 天计算,也可选择**每月 1 号(UTC+0)自动重置**);周期到期自动归档为上一轮;「重置当前周期」把当前周期归档为上一轮、从现在开始新周期——之前的消耗不再计入现在的回本计算,**不影响消耗总额统计**。
- **价格编辑改为页内顶部导航切换**:在原来的「任务通知与消耗」设置页顶部新增导航栏,切换「消耗统计 / 价格编辑 / 使用分析」三个视图(不再另开设置分节);价格编辑视图默认展示官方价,点「保存价格与套餐」后立即生效并重算全部金额。
- **模型价格跨 Key 合并**:同一模型在不同 Key 下只保留一行价格,所有使用该模型的调用都按它计费;未激活自定义的模型默认按内置官方价格目录计费,「激活自定义」解锁编辑(基础价 + 峰/谷子档),「添加模型」可手动加入模型。
- **旧账目按低谷价**:0.1/0.2 版本记录的账目(无 `ver` 标记)默认按低谷价计算金额;0.3 起记录的新账目按每次调用的时刻取峰/谷档。
- **移除低价值兜底**:移除安全启动门(safe-start.js / semver.js,版本/冲突/重复挂载检测)与会话折叠里 `assistant/message` 用量兜底、设置分节错误边界等触发概率极低的兜底逻辑,插件更精简。
- 配置结构:pricing 由 `byKey` 改为 `models`(模型聚合);旧 0.3 配置自动迁移合并。
- **修复模型归纳**:消耗统计页的「按模型消耗总览」改为按**模型名**聚合——同一模型名无论出现在哪个 Key / 提供商下都合并为一行(与价格编辑页「同一模型只有一行价格」、使用分析饼图口径一致);不同版本(不同模型名)各占一行,价格互不影响。按 Key 表仍严格按 Key 归集该 Key 自己的模型明细,不做跨 Key 归纳。

## 0.3.0

- 价格计算:每个 Key × 模型可自定义价格,内置官方价格目录一键填充,峰谷计价。
- Token Plan 订阅回本判断与 Credit 换算。
- 按模型消耗总览(跨 Key 聚合)。
