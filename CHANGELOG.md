# 更新日志 (Changelog)

## dsh-task-notice 0.2.0(2026-09-07)

### 新增

- **操作提醒**:代理发起**权限请求**(批准/拒绝)或**提问**(等待你回答)时,同样触发页内弹窗 + 系统通知提醒你及时处理(遵循「浏览器系统通知」与「仅后台时发送」的设置)。设置页新增两个独立开关:
  - **权限请求提醒**(`notifyOnApproval`,默认开):代理请求权限时通知你处理;
  - **提问提醒**(`notifyOnQuestion`,默认开):代理提问、需要你回答时通知。
- README 补充操作提醒功能说明、两个新配置项,并新增 License 章节(MIT)。

### 修复

- **修复操作提醒导致的审批/提问对话框回归(0.2 开发版缺陷)**:早期实现与官方审批/提问 UI 一样在 `ctx.remote.$on` 上监听 `approval/request`、`user-questions/request` 事件——这两个事件是 agent 作用域上的 **waterfall 应答事件**,任何监听者若不调用 `next()` 就会截断应答链,导致授权/提问对话框不再出现。0.2 正式版已**彻底移除该事件监听**,改为订阅官方 UI 的只读待处理交互存储 `uiSession.pendingInteractions`(请求到达时由审批/提问 UI 发布进入、回答/关闭后自动移除):插件只读快照、订阅变化,**完全不参与应答链,绝不干扰审批/提问对话框**。
- 补漏提醒:监听建立前已挂起的请求(如页面重连后仍在等待回答的交互)会在建立监听时补一次提醒,不再漏报。

### 其他

- 版本号 `0.1.0` → `0.2.0`(`package.json`、`lib/index.js` 的 `PLUGIN_VERSION` 同步更新)。
- 客户端操作提醒逻辑重构:新增 `describePending(interaction)` 统一识别审批/提问两类待处理交互;去重键加入 `sessionId`,同一请求 60 秒内仅提醒一次;去重缓存上限由 64 提升至 128。

## dsh-task-notice 0.1.0(2026-09-06,初始版本)

- **完工通知**:每个顶层会话(非 subagent)完成一轮任务、或目标(goal)完成时,页内 toast + 经浏览器 Web Notifications API 发送 Windows 系统通知(Edge 横幅 + 操作中心,点击回到本页),展示本轮消耗(缓存输入 / 缓外输入 / 输出 / 合计)。
- **按 API Key 的 Token 消耗统计**:支持 1 年 / 6 个月 / 3 个月 / 1 个月 / 15 天 / 1 周 / 24 小时 / 自定义时间范围;按 Key 汇总(调用次数与各类 tokens)与按模型明细、跨 Key 按模型消耗总览。
- **安全启动**:启动时检测 DSH 版本兼容性、冲突插件、重复挂载、服务命名空间占用,任一命中即停用本插件(仅保留健康查询),保证 DSH 照常启动;宿主入口零顶层外部 import,异常只让本插件失效。
- **设置页**:插件总开关、每轮任务弹窗、目标完成弹窗、弹窗时长、浏览器系统通知、仅后台时发送、账本保留天数等配置。
