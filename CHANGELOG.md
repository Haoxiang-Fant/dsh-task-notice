# Changelog

## 0.3.6 (2026-09)

- **顶部提示横幅(页面顶部常驻,shell.overlay 帧级浮层,所有页面显示,居中于页面顶部,可 × 关闭)**:
  - **通知权限横幅**:系统通知开关开启但浏览器尚未授权 Web Notifications 时,页面顶部显示横幅提示「开启权限获得完整能力体验」;**点击横幅即自动执行索权动作**(浏览器弹出授权询问,授权成功后自动开启系统通知),无需再进设置页点「授权并开启」。权限被浏览器拒绝(denied)时横幅改为提示到地址栏站点权限中允许;点击横幅同样会尝试请求(被拒状态浏览器不再弹询问,按提示操作后刷新页面即可)。
  - **模型价格横幅**:「当前使用的模型」没有规定价格(未激活自定义、官方目录未收录、也没有 Provider 专属价)时,页面顶部显示横幅提示「设置自定义价格」;**点击横幅直接弹出该模型的价格编辑弹窗**(五个单价字段,预填全局默认价,留空字段继续继承全局默认价),点「**保存并确定**」即写入配置并立即生效——消耗统计 / 使用分析 / 完工通知金额马上按新价格计算,横幅随之消失(若还有其他未定价的在使用模型则自动换成下一条)。
  - 「当前使用的模型」判定:最近一次完工通知帧携带的模型(实时)∪ 最近 7 天账本中最新使用且未定价的模型(刷新页面后仍能提示);目录未加载或插件停用时不显示。
- **deepseek-flash 价格 = deepseek-v4.1-flash**:内置官方价目录为 deepseek-v4.1-flash 增加 `deepseek-flash` 别名(目录 match 列表),模型名 deepseek-flash 的调用按 deepseek-v4.1-flash 官方价计费(缓存命中 ¥0.02 / 缓外输入 ¥1 / 输出 ¥4,每 1M tokens);生成器 tools/build-catalog.mjs 同步更新,重跑不会丢失。
- 价格工具函数(PRICE_KEY_LIST / cleanPrice / lookupClientModel / officialBaseOf)提升为浏览器端模块级共用(价格编辑页与横幅价格弹窗同源,行为不变)。
- **横幅字色实时对比度修复**:横幅背景是「强调色 × 主题底色」的 color-mix 混色,主题 label 色不保证与之形成对比(部分主题下字色与背景融为一体)。现改为**渲染后读取横幅实际背景色实时决定字色**(YIQ 亮度:浅底 ≥140 取深字 `#1f1f1f`,否则浅字 `#fff`,写入 `--tn-banner-fg` 覆盖主题色);主题切换时经 MutationObserver 观察 body style 变化**实时重算**,每次渲染后亦重算;背景解析失败或全透明时保留主题色。横幅内文字与 × 关闭按钮同用该字色。**横幅同时加大**:字号 12→13px(加粗 500)、内边距 7×12 → 10×16、图标 14→17px、元素间距 10→12、最大宽度 720→860px、圆角 10→12。
- **权限横幅授权回执**:点击权限横幅、浏览器授权成功后,**立即直发一条系统通知**「系统通知已开启」(桌面横幅 + Windows 操作中心,点击回到页面)作为确认回执。直发不经 showNativeNotify:它的「仅后台时发送」会在页面可见时把这条压掉,而这是用户手点的确认信号;updateConfig 失败不影响回执(权限已授予,与开关无关)。
- **修复「点击权限横幅没有反应」(静默根因:授权弹窗被关闭且未选择)**:`requestPermission()` 在用户直接关掉浏览器授权弹窗时返回 `default`——权限没变,此前页面侧对此**毫无可见变化**(横幅文案不变、无任何弹窗),与「点击失效」无法区分。现在点击后**任何结局都立即可见**:
  - **等待中**:横幅文字立即变为「正在请求浏览器授权…请选择允许」(也证明点击已送达——若点击后连这个文字都不变,说明页面还在跑旧代码,刷新即可);
  - **关闭未选择**(default):横幅文字变为「授权窗口被关闭且未做选择…请再次点击并选择允许;若浏览器不再弹出询问,说明已被静默拦截,请在地址栏站点设置中改为允许」;
  - **拒绝**(denied):保留原地址栏提示;
  - **授权成功**:横幅消失 + 直发「系统通知已开启」系统通知回执。
  - 请求期间防重复点击;用户随后在地址栏站点设置手动改为「允许」后,窗口聚焦时横幅自动消失。
- 版本号 0.3.6。

## 0.3.5 (2026-09)

- **修复「偶发需要回答 / 需要授权时没有弹出通知」(根因:去重键误用内容签名)**:操作提醒(提问 / 授权)此前按**内容签名**(问题 id 拼接、工具名+原因)做页内去重且 60 秒不重弹——而问题 id 是模型自选、复用极常见(confirm / mode 等),用户回答后代理再用相同 id 提问(或无 callId 的同类工具连续请求授权)时,新请求被静默吞掉,页内弹窗与系统通知都不发。现改用官方 UI 待处理交互自带的**每请求唯一 key** 做去重身份,请求离开待处理列表(已回答 / 已关闭)即清出去重表,同一仍挂起的请求保留 60 秒防抖;帧 id 随之每请求唯一,同时消除同内容两请求并存时弹窗列表 React key 冲突的隐患。
- **跨标签页系统通知去重与帧 id 解耦**:系统通知的跨标签页 30 秒去重改用独立的**内容签名 dedupeKey**(各标签页交互计数器独立,帧 id 已不再跨页一致);问题签名并入首题文本,仅**完全相同文本**的提问 30 秒内合并横幅(防刷屏底线,页内弹窗每次必弹),仅复用 id、文本不同的新提问不再被吞。
- **修复插件先于 uiSession / slots 就绪时操作提醒整体失效**:浏览器端此前在 apply 里一次性 `ctx.get('uiSession')` / `ctx.get('slots')`,加载时序靠前时提问 / 授权监听或弹窗插槽注册被静默跳过,该页面生命周期内不再弹通知;现声明 `inject: ['remote', 'uiSession', 'slots']`,由 Cordis 停靠等待服务就绪后再激活(与官方 UI 插件一致)。
- **修复断线重连后完工通知静默失效**:`subscribeNotifications` 通知流随连接断开结束后此前不再重订阅,重连后完工 / Token Plan 通知全部失效直到刷新页面;现连接重置时重拉通知流(pumping 守卫防止并发双订阅产生重复帧)。
- 健康信息中插件版本号修正为 0.3.5(常量此前残留 0.3.4)。
- **价格编辑页重设**:改为「模型卡片 + Provider ID 子卡片」结构——每张卡片显示一个模型的名字与集体价格(未自定义时显示官方价,勾选「自定义价格」解锁编辑),卡片右下角「保存 / 设置」;点击卡片非价格区域展开该模型所在的 Provider ID 子卡片,子卡片带「使用集体价格」开关(默认勾选)与右侧「保存 / 设置」,取消勾选即展开该 Provider 的专属价编辑(自动预填当前生效价)。
- **Provider ID 专属价**:pricing.models 增加 providers 段({ useCollective, price }),priceFor 以 Provider ID(账目归一身份)解析 专属价 → 集体价 → 官方价;消耗统计 / 使用分析 / 单次明细 / 完工通知全链路计价点改按 Provider 身份解析。
- **主体峰谷规则(条件格式式)**:卡片 / 子卡片的「设置」按钮进入该主体(模型集体 或 模型 × Provider ID)的峰谷计价临时页:启用开关 + 可增删的规则列表(峰时段 / 仅工作日 / 峰时倍率),自上而下首个命中调用时刻的规则按倍率缩放基础价,无命中按基础价;主体规则优先于官方目录内嵌峰谷档,Provider 规则优先于集体规则;退出临时页即保存此页的一切更改。启用峰谷计价后对应卡片显示「峰谷」徽标与规则摘要(峰谷编辑样式);全局峰谷窗口(官方目录档取档)保留在页首卡片。
- **价格目录换源《大模型API定价对比(4).csv》**:内置官方价目录按参考数据 CSV 重建(214 条;列映射:输入价格→缓外输入、输出价格→输出、缓存命中输入→缓存命中、缓存写入→缓存写入、推理过程→推理过程);DeepSeek 官方「(空闲)/(高峰)」成对行内嵌为峰谷子档(谷 = 基础价);0.3.4 目录中 CSV 未收录的 45 条保留为补充(标「0.3.4 目录补充」);USD 价按固定汇率 7.2 由 CNY 推导。目录文件由 tools/build-catalog.mjs 从 CSV 生成,改价后可重跑。
- 服务端新增 normalizePeakRules;typert pricing schema 扩展 providers / peak(主体规则)段;旧配置读取兼容(无新字段时行为与 0.3.4 一致)。
- **性价比计算表删除「品牌」列**(排名数据不变,仅不再展示);**每日费用消耗表**的模型图例与堆叠色条改按模型名全局分配颜色——同一模型跨日期颜色一致,且与饼图图例同色。
- 版本号 0.3.5。

## 0.3.4 (2026-09)

- **Token Plan 提示重置(402/429)**:模型调用流抛错且错误属于 Token Plan 错误码(HTTP 402/429 或 DSH 归一化码 QUOTA/RATE_LIMIT,沿 cause 链识别)时,若该 Provider 启用了 Token Plan,自动标记该 Provider ID 待确认(持久化 plan.pending)并弹出「需要重置 Token Plan 吗?」;弹窗内嵌重置流程(4 位验证码 + 两次确认,即进入 Token Plan 重置页),「稍后」或未回应不阻塞。
- **续费确认弹窗**:待确认的 Provider 下次正常调用时弹出「是否已续费?」——「是 + 确认」执行续费重置(冻结旧周期并开启新周期、清除待确认),随后再弹「是否更改了本次套餐?」,选「是」切到 设置 → Token Plan Key 管理 修改套餐(订阅费 / Credit / 有效期 / 自动重置);「否 + 确定」仅清除待确认、不重置周期。
- **临时账本**:429/402 之后该 Provider 产生的消耗进入临时账本——不参与当前周期回本 / 性价比计算,每日费用表按「待确认」标记展示;用户回应后:续费(重置)时临时账本并入被冻结的原周期合并显示,未续费时并回当前周期统计。
- **周期唯一 TID**:每个计费周期分配唯一 TID(插件内部身份,不向用户显示),随配置持久化;重置 / 自动轮换时旧 TID 冻结(tokens/cost 落盘,此后不再变化)并写入周期历史。
- **性价比 / 回本只统计当前生效 TID**:性价比排名不再优先用上一轮周期,Token Plan 的 Key 一律按当前周期(当前 TID)加权平均分配订阅费,无周期数据时才回退展示范围并标注「正在统计」。
- **历史周期按钮**:Token Plan 回本卡片新增「历史」按钮,点击展开查阅全部已冻结周期(起止时间 / 订阅费 / tokens / 金额)。
- **每日费用消耗表同 TID 加权**:Token Plan 加权平均严格限定在同一 TID 内(各 TID 用各自「订阅费 ÷ 该 TID 周期总 tokens」费率);同一天同一 Provider 出现在不同 TID 时,当天各 TID 数据相加合并为同一 Provider 显示。
- **撤回保留 TID**:重置撤回(undoPlanCycle)恢复周期边界的同时恢复原 TID,并把该 TID 的冻结快照移出周期历史;服务端与客户端保存套餐时原样保留 tid / cycles / pending / _undo(修复 0.3.3 保存套餐会丢失撤回快照的隐患)。
- 服务端新增 RPC:`renewPlanCycle(key, planChanged, fromMs, toMs)`(续费确认重置)/ `dismissPlanPending(key, fromMs, toMs)`(未续费清除待确认);通知流新增 `plan` 帧类型(plan.action = reset / renew);客户端 CONTRIBUTION 与 api 同步声明。
- 版本号 0.3.4。

## 0.3.3.1 (2026-09)

- **修复「今日消耗」页面完全空白**:今日消耗明细表的渲染引用了未定义的 `entries`(应为 `data.entries`),数据就绪后渲染直接抛 ReferenceError,整个设置页变空白;现补上 `entries` 取值,今日消耗正常显示(消费卡片 / 金额占比饼图 / 各模型 tokens 表 / 单次消耗明细)。
- **修复完工通知显示消耗为 0**:会话折叠(fold.js)此前只监听 `assistant/chunk` 事件累计 usage,而当前 DSH 的会话事件格式(v2/v3)已移除该事件——每次模型调用的用量改由结算事件携带(`assistant/message` 的 `data.usage`,或 `assistant/message` / `assistant/attempt` 的 `data.stream` 内 `usage` 记录);现按官方事件格式读取结算用量,完工通知的 tokens 与金额恢复真实值。旧宿主(格式 v0)的 `assistant/chunk` 逐块通道保留兼容,同一会话只认一种来源,不会重复计数。
- **后台统计不受影响**:消耗统计 / 今日消耗 / 使用分析的数据来自 `llm/stream` 瀑布记账(usage.json),与折叠通道独立,账本数据一直正常;本轮仅修复通知链路与其页面渲染。
- **防御悬停越界**:饼图数据刷新(切片变少/清空)时悬停索引可能越界导致渲染崩溃,今日消耗与使用分析两处饼图气泡均加越界防护。
- 版本号 0.3.3.1。

## 0.3.3 (2026-09)

- **顶部导航栏常驻页面顶部**:导航栏 `sticky` 固定在页面顶部,往下翻阅时内容从其下方滚动;滚动超过阈值后页面右侧出现「回到顶部」箭头按钮,点击平滑回到页面顶部。
- **今日消耗新增单次消耗明细**:每次模型调用产生消耗的单条记录(时间 / 模型 / Provider / 缓存输入 / 缓外输入 / 输出 / 合计 / 金额),按时间倒序展示;服务端新增 RPC `getUsageEntries(fromMs, toMs)`,金额按当前价格与峰谷档逐条现算,客户端 CONTRIBUTION 与 typert 清单同步声明(旧宿主无此 RPC 时该明细块自动隐藏,不影响统计)。
- **修复单模型饼图不显示**:SVG 弧线在起点=终点(单模型占满 360°)时整段不渲染,饼图会变成空白;现把整圆切成两个 180° 半环(颜色/悬停信息不变),单模型时显示完整 100% 圆环(今日消耗与使用分析两处饼图同修)。
- **顶部导航新增「今日消耗」标签页(第一位)**:展示今天的模型消耗金额占比饼状图、各模型 tokens 消耗量(缓存输入/缓外输入/输出/合计)与今天的消费情况(总 tokens / 调用次数 / 金额);数据按今日 00:00(本地)到现在的账本计算,金额按当前价格与峰谷档现算,悬停饼图与数字可看精确值。
- **Token Plan 重置保护**:重置按钮按下后需输入随机 **4 位数字验证码**、再**两次点击不同位置的确认按钮**才真正执行重置(防止误触)。重置完成后显示「撤回重置」按钮,同样输入 4 位验证码 + 两次确认后可**撤回重置**:恢复重置前的周期边界,重置期间产生的消耗自动并入恢复的周期(周期成本按账本时间现算)。撤回状态(plan._undo 快照)随配置持久化,重启 DSH 后仍可撤回。
- **每日费用消耗表补图例**:每日记录的每个模型行前新增颜色图例(与当日堆叠色条一致),便于对照。
- 服务端新增 RPC `undoPlanCycle(key, fromMs, toMs)`(撤回重置,返回最新分析);客户端 CONTRIBUTION 与 api 同步声明。
- 版本号 0.3.3。

## 0.3.2 (2026-09)

- **Key 身份 = Provider ID(全局模型设置路由)**:消耗统计与使用分析的「按 Key」行、Token Plan 归属都改以**全局模型设置里的 Provider 路由**为身份(如 scnet / ten / deepseek-official),显示名用该 Provider 的 displayName(如 超算互联网 / DeepSeek);环境变量名(apiKeyEnv,如 SCNET_API_KEY)只作辅助展示。账本旧 key 标签自动迁移/兼容:plans 里按旧 env 名或 Provider 名设置的套餐在读取与启动时归一为 Provider id(目录未就绪时按内置路由提示表推导,查不到的键保留原样供手动删除)。
- **设置页新增「Token Plan Key 管理」**(取代原「价格编辑-Token订阅」):管理所有 Key(Provider 目录 ∪ 消耗记录 ∪ 已有计划键)的 Token Plan 设置(开关 / 订阅费 / Credit / 套餐起效 / 有效期或每月 1 号重置 / 回本进度 / Credit 换算),并支持**删除该 Key 原有的 Token plan 属性**(删除后该 Key 不再按订阅计费,消耗记录与金额保留)。
- **设置页新增「Provider ID 管理」**:插件自维护 Provider 目录(Provider ID + 显示名 + 环境变量名 + 所含模型),提供**重置(重新抓取全局模型设置 + 账本观测并持久化)**与**手动删除失效 Provider ID**(移出目录 + 删除其 Token Plan,重扫不再复活)。
- **首次启动 / 无数据时自动抓取 Provider 目录**:从全局模型设置(设置 → 模型,llm-* 命名空间 + 已注册 provider 路由)获取所有已配置 Provider 的 ID / 显示名 / 所含模型;价格编辑页在无消耗记录时也列出 Provider 目录所含模型,可提前定价。
- **顶部导航按钮顺序调整**:设置 → 任务通知与消耗 页顶部导航顺序改为 消耗统计 → 使用分析 → 价格编辑 → 设置。
- 配置结构新增 `providers` 覆盖段({ version, updatedAt, removed, list }),由宿主维护;`plans` 键以 Provider id 为准(读取侧归一,旧 env 标签兼容)。
- 服务端新增 RPC:`getProviderDirectory()` / `refreshProviderDirectory()` / `deleteProviderId(id)`;统计与使用分析行携带 `keyEnv` / `providerName` 辅助展示字段。

## 0.3.1 (2026-09)

- **顶部导航新增「设置」选项卡**:原「消耗统计」视图中的插件配置项(插件总开关 / 每轮任务弹窗 / 目标完成弹窗 / 权限请求提醒 / 提问提醒 / 弹窗时长 / 浏览器系统通知 / 仅后台发送 / 账本保留天数)移至「设置」视图;新增**清除记录数据**——图形验证码滑块拖到底(锁定变绿)→ 输入随机 8 位英文数字验证码 → 连续点击两次确认才执行清除(服务端 clearUsage 清空 usage.json,价格/套餐/峰谷配置保留;清除后统计与分析自动刷新;宿主进程未重启导致 remote 缺 clearUsage 时,面板显示明确提示而非报错)。
- **修复消耗归因「张冠李戴」**:旧版按 provider 硬编码命名空间取 apiKeyEnv,把 scnet 等 provider 的调用误记到共享命名空间(llm-deepseek)的 DEEPSEEK_API_KEY 名下;现按**真实路由配置**解析(扫描 settings 的 llm-* 命名空间 providers[route].apiKeyEnv → llm-<route> 扁平命名空间 → 路由专属环境变量),启动时对历史账本**按新解析器重算一次归属**(幂等),每次调用记录的模型名与 Key 均为该次调用实际使用的值。
- **默认价格目录对齐《主流大模型API官方价格汇总.xlsx》(2026-09)**:内置官方价目录扩至 80+ 模型,表格已收录的模型默认价一律按表格取值(输入价→缓外输入、缓存命中价→缓存命中、输出价→输出;区间价取基础起步价;DeepSeek 峰档 = 2× 谷档),并新增字节 Seed/Doubao、百度 ERNIE、腾讯混元、智谱 GLM-4-Plus / 4-Air / 4.7-Flash(免费)、月之暗面 K3 / K2.6 / K2.5 / Moonshot、阿里 Qwen3.7-Max / 3.6-Flash、小米 MiMo、零一 Yi-Lightning、阶跃 Step-3.7-Flash、百川 Baichuan-M3-Plus、讯飞 Spark、OpenAI GPT-5.4-Nano、Claude Fable 5 / Opus 5 / Sonnet 5、Gemini 3.6 / 3.5 Flash 系列等条目;DeepSeek V3.1 / R1 独立成行;表格未收录的既有官方价条目保留补充。
- **顶部导航栏新增「使用分析」界面**:在原「消耗统计 / 价格编辑」导航基础上新增「使用分析」页,分析规定时间内各大模型的使用资金状况,包含四个模块:
  - **消耗金额展示**:模型消耗金额占比**饼状图**(鼠标悬停饼图块浮现该模型具体使用情况:调用次数、各类 tokens、金额与占比)+ 饼图下方各 Key 消耗额度表格;
  - **Token Plan 回本提示**:每个设置为 Token Plan 的 Key 一张独立子卡片(Key 名称、名下每个模型的消耗金额、是否回本、每 Credit 对应 tokens、**重置当前周期**按钮);
  - **性价比计算排名**:所有模型的 **(Key · 模型)** 一起排名比较(排名名 = Key · 模型),按 1 元(CNY)能兑换多少 tokens 从高到低排序;**Token Plan 的模型按「订阅费 × 该模型 tokens ÷ 周期总 tokens」加权平均分配额度**(基于 Tokens 消耗摊薄订阅费用),优先用**上一轮计费周期**,无上一轮数据时用当前周期并标注「正在统计」;
  - **每日费用消耗表**:**微型日历**中代表一天的正方形填充颜色深度区分每天用量,日历下方列出每个日期的各模型消耗金额(Token Plan 模型按**加权平均**计算:订阅费 ÷ 周期总 tokens × 当天 tokens),数据默认堆叠,一次显示最近 6 天,滚轮滚动查看其余。
- **Token Plan 计费周期**:设置为 Token Plan 时需设置**套餐起效时间**与**有效期**(默认按 31 天计算,也可选择**每月 1 号(UTC+0)自动重置**);周期到期自动归档为上一轮;「重置当前周期」把当前周期归档为上一轮、从现在开始新周期——之前的消耗不再计入现在的回本计算,**不影响消耗总额统计**。
- **价格编辑改为页内顶部导航切换**:在原来的「任务通知与消耗」设置页顶部新增导航栏,切换「消耗统计 / 价格编辑 / 使用分析」三个视图(不再另开设置分节);价格编辑视图默认展示官方价,点「保存价格与套餐」后立即生效并重算全部金额。
- **模型价格跨 Key 合并**:同一模型在不同 Key 下只保留一行价格,所有使用该模型的调用都按它计费;未激活自定义的模型默认按内置官方价格目录计费,「激活自定义」解锁编辑(基础价 + 峰/谷子档),「添加模型」可手动加入模型。
- **旧账目按低谷价**:0.1/0.2 版本记录的账目(无 `ver` 标记)默认按低谷价计算金额;0.3 起记录的新账目按每次调用的时刻取峰/谷档。
- **移除低价值兜底**:移除安全启动门(safe-start.js / semver.js,版本/冲突/重复挂载检测)与会话折叠里 `assistant/message` 用量兜底、设置分节错误边界等触发概率极低的兜底逻辑,插件更精简。
- 配置结构:pricing 由 `byKey` 改为 `models`(模型聚合);旧 0.3 配置自动迁移合并。
- **修复模型归纳**:消耗统计页的「按模型消耗总览」改为按**模型名**聚合——同一模型名无论出现在哪个 Key / 提供商下都合并为一行(与价格编辑页「同一模型只有一行价格」、使用分析饼图口径一致);不同版本(不同模型名)各占一行,价格互不影响。按 Key 表仍严格按 Key 归集该 Key 自己的模型明细,不做跨 Key 归纳。

## 0.3.0

- 价格计算:每个 Key × 模型可自定义价格,内置官方价格目录一键填充,峰谷计价。
- Token Plan 订阅回本判断与 Credit 换算。
- 按模型消耗总览(跨 Key 聚合)。
